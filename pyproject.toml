[project]
name = "dynamic_graphs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Susanne Waschk", email = "Waschk.Susanne@outlook.com" }
]
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "ipykernel>=7.3.0",
    "matplotlib>=3.11.1",
    "pandas>=3.0.5",
]

[project.scripts]
dynamic_graphs = "dynamic_graphs:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
